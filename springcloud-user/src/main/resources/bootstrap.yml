
# consul 服务注册
spring:
  application:
      name: user-service
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: false # 是否开启consul的kv配置
      discovery:
        health-check-interval: 30s



# eureka服务注册配置
#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8761/eureka/