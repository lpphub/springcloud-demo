
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lsk12162015/config-repo

# eureka服务注册配置
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/